import React from "react";

// TODO: Delete Later. Only used for placeholder
export default function PlaceholderComponent({ className }) {
  return (
    <div className={`${className} flex items-center justify-center`}>
      Placeholder Component
      <div></div>
    </div>
  );
}
